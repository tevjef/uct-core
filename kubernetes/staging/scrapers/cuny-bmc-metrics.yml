apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: monitoring
  name: cuny-bmc
  labels:
    metrics: cuny-bmc
spec:
  jobLabel: cuny-bmc
  selector:
    matchLabels:
      app: cuny-bmc
  namespaceSelector:
    matchNames:
    - staging
  endpoints:
  - port: uct-metrics
    interval: 2m
    honorLabels: true
---
apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: cuny-bmc
  labels:
    app: cuny-bmc
spec:
  selector:
    app: cuny-bmc
  ports:
  - name: uct-metrics
    port: 13000
    protocol: TCP
    targetPort: uct-metrics