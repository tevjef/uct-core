apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: cuny-lag
  labels:
    app: cuny-lag
spec:
  selector:
    app: cuny-lag
  ports:
  - name: uct-metrics
    port: 13000
    protocol: TCP
    targetPort: uct-metrics
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: staging
  labels:
    app: cuny-lag
  name: cuny-lag
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cuny-lag
    spec:
      containers:
        - name: cuny-lag
          image: docker.io/tevjef/uct-core:cuny-staging
          ports:
          - containerPort: 13000
            name: uct-metrics
            protocol: TCP
          imagePullPolicy: Always
          env:
          - name: CUNY_UNIVERSITY
            value: LAG
          - name: JET_DAEMON
            value: 10m
          - name: JET_DAEMON_JITTER
            value: "50"
          - name: JET_SCRAPER_NAME
            value: cuny-lag
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: monitoring
  name: cuny-lag
  labels:
    metrics: cuny-lag
spec:
  jobLabel: cuny-lag
  selector:
    matchLabels:
      app: cuny-lag
  namespaceSelector:
    matchNames:
    - staging
  endpoints:
  - port: uct-metrics
    interval: 2m
    honorLabels: true
