apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: monitoring
  name: cuny-kcc
  labels:
    metrics: cuny-kcc
spec:
  jobLabel: cuny-kcc
  selector:
    matchLabels:
      app: cuny-kcc
  namespaceSelector:
    matchNames:
    - staging
  endpoints:
  - port: uct-metrics
    interval: 2m
    honorLabels: true
---
apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: cuny-kcc
  labels:
    app: cuny-kcc
spec:
  selector:
    app: cuny-kcc
  ports:
  - name: uct-metrics
    port: 13000
    protocol: TCP
    targetPort: uct-metrics
