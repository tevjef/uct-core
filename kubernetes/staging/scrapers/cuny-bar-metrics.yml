apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  namespace: monitoring
  name: cuny-bar
  labels:
    metrics: cuny-bar
spec:
  jobLabel: cuny-bar
  selector:
    matchLabels:
      app: cuny-bar
  namespaceSelector:
    matchNames:
    - staging
  endpoints:
  - port: uct-metrics
    interval: 2m
    honorLabels: true
---
apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: cuny-bar
  labels:
    app: cuny-bar
    app: cuny-bar
spec:
  selector:
    app: cuny-bar
  ports:
  - name: uct-metrics
    port: 13000
    protocol: TCP
    targetPort: uct-metrics