# gazelle:prefix github.com/tevjef/uct-backend

load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    command = "fix",
    external = "vendored",
    prefix = "github.com/tevjef/uct-backend",
    extra_args = [
            "-proto",
            "disable",
        ],
)